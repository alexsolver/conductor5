// TEMPORARILY DISABLED - Custom fields repository
// This module has been temporarily disabled due to schema import conflicts
// It will be re-enabled once the custom fields schema is properly integrated

export class CustomFieldsRepository {
  constructor() {
    console.log('⚠️  Custom Fields Repository temporarily disabled');
  }
}

export default CustomFieldsRepository;
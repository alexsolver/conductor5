Baseado na análise completa do código do projeto, identifiquei múltiplas inconsistências críticas no schema do banco de dados. Aqui está a análise detalhada:

🔍 ANÁLISE COMPLETA DE INCONSISTÊNCIAS NO SCHEMA
1. ARQUITETURA FRAGMENTADA CRÍTICA
1.1. Múltiplos Arquivos de Schema Conflitantes
shared/schema.ts - Schema principal com estrutura consolidada
server/db.ts - Lógica de criação de tabelas via SQL raw
server/db-unified.ts - Schema alternativo com estrutura diferente
server/modules/shared/database/SchemaManager.ts - Queries SQL hardcoded
PROBLEMA CRÍTICO: Não há um ponto único de verdade para o schema.

2. INCONSISTÊNCIAS DE TIPOS DE DADOS
2.1. IDs Inconsistentes Entre Arquivos
// Em shared/schema.ts
tenantId: uuid("tenant_id").notNull()
// Em server/db.ts (linha 578)
tenant_id VARCHAR(36) NOT NULL
// INCONSISTÊNCIA: UUID vs VARCHAR(36)
2.2. Campos JSONB vs TEXT
// shared/schema.ts
metadata: jsonb("metadata").default({})
// server/modules/.../SchemaManager.ts (linha 89)
metadata TEXT
3. PROBLEMAS DE ESTRUTURA DE TABELAS
3.1. Conflito customers vs solicitantes
// shared/schema.ts define 'customers' com campos brasileiros
documento: varchar("documento", { length: 50 })
tipoPessoa: varchar("tipo_pessoa", { length: 20 })
// server/db-unified.ts define 'solicitantes'
solicitantes: { documento, tipo_pessoa }
// CONFLITO: Duas tabelas para a mesma entidade
3.2. Tabela favorecidos Definida Múltiplas Vezes
-- Em server/db.ts (linhas 250-267)
CREATE TABLE favorecidos (
  id UUID PRIMARY KEY,
  tenant_id VARCHAR(36)
)
-- Em shared/schema.ts 
favorecidos = pgTable("favorecidos", {
  id: uuid("id").primaryKey(),
  tenantId: uuid("tenant_id")
})
4. INCONSISTÊNCIAS DE FOREIGN KEYS
4.1. Relacionamentos Órfãos
// tickets referencia customers
customerId: uuid("customer_id").notNull()
// Mas algumas implementações usam solicitante_id
solicitante_id UUID REFERENCES solicitantes(id)
4.2. Foreign Keys Inconsistentes
-- Em alguns locais
FOREIGN KEY (customer_id) REFERENCES customers(id)
-- Em outros locais sem referência adequada
customer_id UUID -- sem REFERENCES
5. PROBLEMAS DE VALIDAÇÃO E CONSTRAINTS
5.1. Constraints de tenant_id Inconsistentes
-- server/db.ts
CONSTRAINT customers_tenant_id_format CHECK (LENGTH(tenant_id) = 36)
-- Mas nem todas as tabelas têm essa validação
5.2. Unique Constraints Conflitantes
-- Algumas tabelas
UNIQUE (tenant_id, email)
-- Outras tabelas
UNIQUE (email) -- Pode causar problemas multi-tenant
6. PROBLEMAS DE ÍNDICES
6.1. Índices Duplicados ou Faltantes
-- Definidos múltiplas vezes
CREATE INDEX customers_tenant_email_idx
-- Faltam índices críticos para performance
-- Relacionamentos sem índices adequados
7. INCONSISTÊNCIAS DE CAMPOS OBRIGATÓRIOS
7.1. tenant_id Opcional vs Obrigatório
// shared/schema.ts
tenantId: uuid("tenant_id").notNull()
// server/modules/SchemaManager.ts
tenant_id VARCHAR(36) -- sem NOT NULL em alguns casos
8. PROBLEMAS DE MIGRAÇÃO AUTO-HEALING
8.1. Lógica Conflitante
// server/db.ts tem auto-healing que pode conflitar
await this.migrateLegacyTables(schemaName)
// Mas shared/schema.ts não prevê essa migração
8.2. Validação de Tabelas Inconsistente
// Valida 17 tabelas em alguns locais
const requiredTables = ['customers', 'tickets'...] // 17 itens
// Mas apenas 6 em outros
table_name IN ('solicitantes', 'favorecidos', 'tickets'...) // 6 itens
9. INCONSISTÊNCIAS DE NOMENCLATURA
9.1. Padrões Mistos
// Português vs Inglês
'solicitantes' vs 'customers'
'favorecidos' vs 'external_contacts'
9.2. Convenções de Naming
-- Alguns com underscores
customer_companies
-- Outros com camelCase no código
customerCompanies
10. PROBLEMAS DE CONFIGURAÇÃO DRIZZLE
10.1. Schema Path Inconsistente
// drizzle.config.ts aponta para
schema: "./shared/schema.ts"
// Mas sistema usa múltiplos schemas
11. ERRO CRÍTICO ATUAL
11.1. Unterminated String Literal
ERROR: Unterminated string literal
/server/index.ts:1:71
CAUSA: Provavelmente aspas não fechadas em algum arquivo de configuração do schema.

RECOMENDAÇÕES CRÍTICAS
🚨 CONSOLIDAÇÃO URGENTE: Escolher shared/schema.ts como única fonte de verdade
🔧 PADRONIZAÇÃO: UUID para todos os IDs, JSONB para metadados
🛡️ INTEGRIDADE: Implementar foreign keys consistentes
📊 PERFORMANCE: Criar índices padronizados tenant-first
🏗️ MIGRAÇÃO: Sistema de migração controlado
✅ VALIDAÇÃO: Constraints obrigatórios para isolamento multi-tenant
